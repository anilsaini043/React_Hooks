## useState

## useEffect
> Use to perform the side effect of the component
> Run on every render or update when we dont use any dependency array
> With empty array runs only once when page load
> Cleanup function 
> Help to call API's

## useRef
> "current" holds the real element/value inside the ref.

## useMemo
> When to use
    > Expensive calculation
    > Avoiding unnecessary renders
    > Dependency based memoization

> Don't use over which make debug issue
> Difference between useMemo and React.memo